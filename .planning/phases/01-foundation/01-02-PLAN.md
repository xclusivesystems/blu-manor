---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/app/(site)/layout.tsx
autonomous: true
requirements:
  - SCAF-02

must_haves:
  truths:
    - "Header with navigation links appears at the top of every page"
    - "Mobile menu opens and closes on small screens with Menu/X icon toggle"
    - "Footer with 4-column grid appears at the bottom of every page"
    - "Navigation links match the 8 pages: Home, About, Housing, Program, Partners, Resources, Contact, Apply"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky header with desktop nav and mobile menu"
      contains: "useState"
    - path: "src/components/layout/Footer.tsx"
      provides: "4-column footer with brand, links, resources, contact"
      contains: "XclusiveSystems"
  key_links:
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import and render"
      pattern: "import.*Header"
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/layout/Footer.tsx"
      via: "import and render"
      pattern: "import.*Footer"
    - from: "src/components/layout/Header.tsx"
      to: "navLinks"
      via: "constants import"
      pattern: "import.*constants"
---

<objective>
Build the shared Header (with mobile menu) and Footer components, then wire them into the (site) layout so they appear on every page.

Purpose: Every page in the site needs consistent navigation and footer. This plan delivers the layout shell that all Phase 3 pages will inherit.
Output: Header.tsx and Footer.tsx components rendered in the (site) layout.
</objective>

<execution_context>
@/home/xclusivesystems/.claude/get-shit-done/workflows/execute-plan.md
@/home/xclusivesystems/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@design-system/blu-manor/MASTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Header with mobile menu and Footer components</name>
  <files>
    src/components/layout/Header.tsx
    src/components/layout/Footer.tsx
  </files>
  <action>
    **Header.tsx** — "use client" component:
    1. Import `useState` from React, `Link` from next/link, `Image` from next/image, `Menu` and `X` from lucide-react
    2. Import `navLinks` and `siteConfig` from `@/lib/constants` (created in Plan 03, but import path established now — if constants.ts doesn't exist yet, define a local temporary array of nav links to avoid build errors; Plan 03 will replace it)
    3. Sticky header: `fixed top-0 w-full z-50 bg-bg-surface/95 backdrop-blur-sm border-b border-border`
    4. Desktop nav (hidden on mobile, `md:flex`): render navLinks as `<Link>` elements with `text-muted hover:text-primary transition-[color] duration-200`
    5. Apply CTA button: gold background, links to /apply — `bg-primary text-bg-deep font-semibold rounded-lg px-4 py-2 hover:bg-primary-light transition-[color,background-color,transform] duration-200 hover:-translate-y-0.5`
    6. Logo: `<Image>` component pointing to `/img/logo.png`, width 40, height 40. Next to logo text "Blu Manor" in Libre Baskerville with "Second Chance Housing" subtext in muted
    7. Mobile menu toggle: `<button>` with `Menu`/`X` lucide icons, toggling `mobileOpen` state
    8. Mobile nav panel: `md:hidden` div with `transition-[max-height,opacity] duration-300`, `max-h-screen opacity-100` when open, `max-h-0 opacity-0` when closed, `overflow-hidden`
    9. Mobile nav renders same links vertically with `py-3 border-b border-border` styling
    10. Add `cursor-pointer` to all interactive elements

    **Footer.tsx** — server component (no client interactivity):
    1. Import `Link` from next/link, relevant Lucide icons (Mail, Phone, MapPin)
    2. Import `siteConfig` and `navLinks` from `@/lib/constants`
    3. 4-column grid layout (`md:grid-cols-4 gap-8`):
       - Column 1 (Brand): Logo text "Blu Manor", tagline, brief description
       - Column 2 (Quick Links): Home, About, Housing, Program links
       - Column 3 (Resources): Partners, Resources, Contact, Apply links
       - Column 4 (Contact): Email, both phone numbers (resident + partner), address
    4. Bottom bar: copyright year (dynamic via `new Date().getFullYear()`), "Built by XclusiveSystems" credit per MASTER.md checklist
    5. Background: `bg-bg-surface border-t border-border`
    6. All links: `text-muted hover:text-primary transition-[color] duration-200`

    **Temporary constants fallback:** Since Plan 03 creates constants.ts, but Plan 02 runs in the same wave, create a minimal `src/lib/constants.ts` with just `navLinks` and `siteConfig` exports needed for Header/Footer. Plan 03 will expand this file with the complete content. Define the types inline or in the same file for now.

    **Anti-patterns:**
    - Never use `transition-all` — use specific properties
    - Never use emoji icons — Lucide React only
    - No plain gray shadows — tinted navy only
    - No default Tailwind blue/indigo colors
  </action>
  <verify>
    TypeScript compiles without errors: `npx tsc --noEmit`. Both component files exist and export default functions.
  </verify>
  <done>
    Header.tsx renders sticky nav with logo, desktop links, mobile menu toggle, and Apply CTA. Footer.tsx renders 4-column grid with brand, links, resources, contact, and XclusiveSystems credit. Both components use design system tokens exclusively.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Header and Footer into (site) layout</name>
  <files>
    src/app/(site)/layout.tsx
  </files>
  <action>
    1. Import Header and Footer into `src/app/(site)/layout.tsx`
    2. Render `<Header />` before `{children}` and `<Footer />` after
    3. Add `pt-[72px]` (or appropriate value matching header height) to the main content wrapper so content doesn't hide behind the fixed header
    4. Structure: `<html><body><Header /><main className="min-h-screen pt-[72px]">{children}</main><Footer /></body></html>`
    5. Ensure the body still has both font variable classes applied

    **Note:** The (site)/layout.tsx was created in Plan 01. This task modifies it to add Header/Footer imports and rendering.
  </action>
  <verify>
    Run `npm run dev` and visit `http://localhost:3000`:
    - Header visible at top with nav links and mobile menu
    - Footer visible at bottom with 4-column layout
    - Resize to mobile (375px) — mobile menu toggle appears, hamburger icon works
    - Content area is not hidden behind the fixed header
  </verify>
  <done>
    Header and Footer appear on every route under the (site) route group. Mobile menu opens/closes correctly. Content is properly offset below the fixed header.
  </done>
</task>

</tasks>

<verification>
1. Header renders at top of page with logo, navigation links, and Apply CTA
2. Mobile menu opens on hamburger click, shows all nav links, closes on X click
3. Footer renders with 4-column grid at bottom of page
4. Footer contains "Built by XclusiveSystems" credit
5. Both components use only design system tokens (no Tailwind blue/indigo, no transition-all)
6. Navigation links are: Home, About, Housing, Program, Partners, Resources, Contact, Apply
</verification>

<success_criteria>
Header and Footer appear on every page route. The mobile menu is functional. All navigation links are present. The layout shell is complete and ready for page content in Phase 3.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
