---
phase: 03-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(site)/housing/page.tsx
  - src/app/(site)/program/page.tsx
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04

must_haves:
  truths:
    - "Housing page shows listing card(s) with price, features, availability badge, and Apply CTA"
    - "Housing page uses housing.map() so adding options requires zero code change"
    - "Program page shows services, house rules, what-to-expect timeline, and FAQ link"
    - "Both pages are responsive at 375px with no horizontal scroll"
  artifacts:
    - path: "src/app/(site)/housing/page.tsx"
      provides: "Housing listings page"
      contains: "housing.map"
    - path: "src/app/(site)/program/page.tsx"
      provides: "Program details page"
      contains: "services"
  key_links:
    - from: "src/app/(site)/housing/page.tsx"
      to: "src/lib/constants.ts"
      via: "import { housing, siteConfig }"
      pattern: "import.*housing.*constants"
    - from: "src/app/(site)/program/page.tsx"
      to: "src/lib/constants.ts"
      via: "import { services, faq, siteConfig }"
      pattern: "import.*services.*constants"
---

<objective>
Build the Housing and Program pages — the core content pages explaining what Blu Manor offers and how the program works.

Purpose: Housing is the primary conversion page for prospective residents evaluating options. Program provides the detail needed for both residents and referring partners to understand rules and expectations.

Output: Two fully styled, responsive pages with content from constants.ts and Framer Motion animations.
</objective>

<execution_context>
@/home/xclusivesystems/.claude/get-shit-done/workflows/execute-plan.md
@/home/xclusivesystems/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pages/03-RESEARCH.md
@src/lib/constants.ts
@src/components/ui/AnimatedSection.tsx
@src/components/ui/Button.tsx
@src/components/ui/Card.tsx
@src/components/ui/SectionHeader.tsx
@design-system/blu-manor/MASTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Housing page with listing cards and eligibility</name>
  <files>src/app/(site)/housing/page.tsx</files>
  <action>
Create Housing page as a React Server Component with these sections:

1. **Page Banner** — Short hero (`py-24`) with bg-bg-surface and grain class. Title "Our Housing" in Libre Baskerville, subtitle about safe and affordable transitional housing.

2. **Housing Listings** — SectionHeader label="Available Housing" title="Find Your Room". Map `housing[]` using `housing.map()` (currently 1 item, but MUST use .map() for future expansion). Each housing option renders as a large featured card:
   - Price: `text-4xl font-bold text-primary font-[family-name:var(--font-libre)]` with pricePeriod in `text-lg text-muted`
   - Type name (e.g., "Shared Room") as card title
   - Availability badge: green dot (`w-2 h-2 rounded-full bg-green-500 inline-block`) + availability text in `text-sm text-green-400`
   - Features list: map `features[]` as a checklist with CheckCircle Lucide icon (text-primary) per item
   - Button "Apply Now" linking to /apply (primary variant)
   - Use `max-w-2xl mx-auto` for single-card centering, but when multiple cards exist the grid should work (`grid grid-cols-1 md:grid-cols-2 gap-8`)

3. **What's Included** — bg-bg-surface section. SectionHeader label="All-Inclusive" title="Everything You Need". Brief prose about utilities, Wi-Fi, furniture, laundry being included. Use a 2x3 icon grid showing the included amenities (Wifi, Tv, WashingMachine/Shirt, Zap, Bed, Home icons from Lucide). Each item: icon + label text.

4. **Eligibility** — SectionHeader label="Eligibility" title="Who Qualifies". Brief text explaining: adults 18+, must maintain employment or verified income, must comply with supervision requirements. Use a checklist-style layout with Check Lucide icons.

5. **CTA** — "Ready to Apply?" with Apply Now button (primary, /apply) and "Call Us" button (secondary, tel:siteConfig.phoneResident).

Export metadata: `title: "Housing | Blu Manor — Affordable Transitional Housing Tampa Bay"`, appropriate description.

**IMPORTANT:** No `transition-all`. Use design system tokens only. Add `cursor-pointer` on clickable elements.
  </action>
  <verify>Run `npm run build` — compiles without errors. Visit localhost:3000/housing — all sections render with housing card, features, and CTA.</verify>
  <done>Housing page renders listing card(s) via .map(), features checklist, amenities grid, eligibility section, and Apply CTA. Adding a second HousingOption to constants.ts would automatically render a second card.</done>
</task>

<task type="auto">
  <name>Task 2: Build Program page with services, rules, and timeline</name>
  <files>src/app/(site)/program/page.tsx</files>
  <action>
Create Program page as a React Server Component with these sections:

1. **Page Banner** — Short hero (`py-24`) with bg-bg-surface and grain class. Title "Our Program" in Libre Baskerville, subtitle about structured support for successful reentry.

2. **What We Offer** — SectionHeader label="Program Features" title="What We Offer". Map `services[]` (4 items) into a 2x2 grid with Card components, same pattern as Home page services section. Include Lucide icons (Home, Briefcase, Shield, CheckCircle). Stagger AnimatedSection delays.

3. **House Rules** — bg-bg-surface section. SectionHeader label="Expectations" title="House Rules". Static content in a clean list format. Rules from the original program.html:
   - Rent due on agreed-upon date each month
   - Must maintain employment or verified income
   - Must comply with all supervision requirements
   - No illegal substances, drugs, or paraphernalia on premises
   - Respect all residents, staff, and property
   - Curfew compliance (if applicable to supervision terms)
   - Participate in scheduled house meetings
   Use ShieldCheck or ClipboardList Lucide icon per rule item.

4. **What To Expect** — SectionHeader label="Your Journey" title="What To Expect". 4-step timeline (same pattern as Home "How It Works" but with more detail):
   - Step 1: Apply — Submit your application online or call us
   - Step 2: Interview & Review — Brief phone or in-person screening
   - Step 3: Documentation — Complete required paperwork and agreements
   - Step 4: Move In — Get settled in your furnished room
   Grid layout: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8` with numbered step badges.

5. **FAQ Preview** — bg-bg-surface section. Show first 3 items from `faq[]` as static Q&A (question bold, answer below). Include a Button "See All FAQs" linking to /resources.

6. **CTA** — "Ready to Get Started?" with Apply Now (/apply) and Contact Us (/contact) buttons.

Export metadata: `title: "Program | Blu Manor — Transitional Housing Program Details"`, appropriate description.
  </action>
  <verify>Run `npm run build` — compiles without errors. Visit localhost:3000/program — services grid, rules list, timeline, FAQ preview, and CTAs all render.</verify>
  <done>Program page renders services grid, house rules list, what-to-expect timeline, FAQ preview (first 3 items with link to /resources), and CTAs. Content matches original program.html intent.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Housing page at /housing shows listing card with price, features, availability, amenities, eligibility
- Program page at /program shows services, rules, timeline, FAQ preview
- Both pages responsive at 375px
- AnimatedSection scroll reveals work
- housing.map() used (not hardcoded single card)
- No `transition-all` or default blue/indigo
</verification>

<success_criteria>
Housing and Program pages are fully built and responsive. A prospective resident can review housing options, understand the program, see rules and expectations, and navigate to Apply from either page.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages/03-02-SUMMARY.md`
</output>
